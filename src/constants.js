/** Internal identifier: settings key, CSS prefix, log namespace, message.extra key. */
export const SLUG = 'cairn';

/** Folder SillyTavern installs us into, used for template lookups. */
export const EXTENSION_PATH = 'third-party/cairn-memory';

/** Human-facing name. Matches manifest.json display_name. */
export const DISPLAY_NAME = 'Cairn-Memory';
